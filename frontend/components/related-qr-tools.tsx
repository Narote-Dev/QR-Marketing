import Link from "next/link";
import { qrPages, type QrSeoSlug } from "@/lib/seo/site";
export function RelatedQrTools({ slugs }: { slugs: string[] }) { return <section className="mt-12" aria-labelledby="related-heading"><h2 id="related-heading" className="text-2xl font-bold tracking-tight">Related QR tools</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{slugs.map((slug) => { const page = qrPages[slug as QrSeoSlug]; return <Link key={slug} href={`/qr-code/${page.slug}`} className="rounded-xl border bg-white p-4 transition hover:border-blue-400 hover:shadow-sm"><h3 className="font-semibold text-blue-700">{page.title}</h3><p className="mt-1 text-sm text-slate-600">Create a QR code for {page.slug === "url" ? "a website link" : page.slug}.</p></Link>; })}</div></section>; }
